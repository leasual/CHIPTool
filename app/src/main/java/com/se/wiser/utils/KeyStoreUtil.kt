package com.se.wiser.utils

import android.content.Context
//import chip.devicecontroller.PreferencesKeyValueStoreManager

object KeyStoreUtil {
    fun getValue(context: Context, key: String): String {
//        val value = PreferencesKeyValueStoreManager(context).get(key)
//        if (value == null || value.isEmpty()) {
//            return ""
//        }
//        return value
        return ""
    }
}